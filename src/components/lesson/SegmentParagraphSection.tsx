import { IonItem, IonText } from "@ionic/react";
import React from "react";
import { LessonSection } from "../../objects/Lesson";
import { ParagraphGroup } from "../../objects/Lesson";
import { translationService } from "../../services/TranslationService";

interface SegmentParagraphSectionProps {
    section?: LessonSection;
    
 }

 export const SegmentParagraphSection: React.FC<SegmentParagraphSectionProps> = ({ section}) => {
    
    return(
        <></>
    )
 }